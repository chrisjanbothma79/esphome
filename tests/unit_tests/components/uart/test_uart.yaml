packages:
  esphome_testing: !include ../main.yaml

esphome:
  name: test-uart
  includes:
    - common.h
    - uart_component.cpp
    - uart_device.cpp

uart:
  port: "/dev/ttyDUMMY"
  baud_rate: 9600

