i2c:
  - id: i2c_hdc302x
    scl: ${scl_pin}
    sda: ${sda_pin}

hdc302x:
  temperature:
    name: grow_a_temp
    id: grow_a_temp
  humidity:
    name: grow_a_humidity
    id: grow_a_humidity
  last_error:
    name: grow_a_last_error
  update_interval: 10s
