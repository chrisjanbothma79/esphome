substitutions:
  uart_tx: GPIO4
  uart_rx: GPIO5
  keep_awake: GPIO3

deep_sleep:
  wakeup_pin: ${keep_awake}
  wakeup_pin_mode: KEEP_AWAKE

button:
  - platform: template
    name: Enter sleep mode
    entity_category: CONFIG
    on_press:
      - deep_sleep.enter: {}

<<: !include common.yaml
