network:

udp:

packet_transport:
  platform: udp
  providers:
    - name: some-device-name
      status_sensor:
        name: "Status sensor"

sensor:
  - platform: packet_transport
    provider: some-device-name
    id: our_id
