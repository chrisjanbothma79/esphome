substitutions:
  pin: GPIO38

packages:
  common: !include esp32-common.yaml

remote_transmitter:
  - id: !extend xmitr
    clock_resolution: 2000000
    one_wire: true
    rmt_symbols: 64
    use_dma: true
