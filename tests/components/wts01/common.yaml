uart:
  id: uart_bus
  tx_pin: ${tx_pin}
  rx_pin: ${rx_pin}
  baud_rate: 9600

wts01:
  id: wts01_sensor
  uart_id: uart_bus

sensor:
  - platform: wts01
    wts01_id: wts01_sensor
    temperature:
      name: "WTS01 Temperature"
