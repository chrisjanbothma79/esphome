esphome:
  name: test

spi:
  id: spi_1
  clk_pin: ${clk_pin_name}
  miso_pin: ${miso_pin_name}
  mosi_pin: ${mosi_pin_name}

sdfs:
  id: sdfs_1
  type: sdspi
  cs_pin: ${cs_pin_name}

  # bus_slot: ${bus_id_name}
  # miso_pin: ${miso_pin_name}
  # mosi_pin: ${mosi_pin_name}
  # clk_pin: ${clk_pin_name}

