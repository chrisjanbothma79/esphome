i2c:
  - id: i2c_sht20
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: sht20
    temperature:
      name: SHT20 Temperature
    humidity:
      name: SHT20 Humidity
    update_interval: 15s
